{% extends 'base.html.twig' %}

{% block title %}Réinitialiser le mot de passe{% endblock %}

{% block body %}
    <h1 class="font-bold text-center text-2xl my-10">Réinitialiser votre mot de passe</h1>

    <div class="flex flex-col my-10 p-5 mx-auto max-w-md rounded-2xl shadow-md shadow-blue-700 items-center space-y-4 font-semibold bg-[#F7F7F7]">
        {{ form_start(resetForm) }}
            <div class="grid grid-cols-3 gap-5 items-center mx-5 text-sm">
                {{ form_label(resetForm.plainPassword.first) }}
                {{ form_widget(resetForm.plainPassword.first, { 'attr': { 'class': 'col-span-2 mt-1 block w-full bg-white border border-blue-700 rounded-md text-black px-2' } }) }}
                {{ form_errors(resetForm.plainPassword) }}
            </div>
            <div class="grid grid-cols-3 gap-5 items-center mx-5 text-sm">
                {{ form_label(resetForm.plainPassword.second) }}
                {{ form_widget(resetForm.plainPassword.second, { 'attr': { 'class': 'col-span-2 mt-1 block w-full bg-white border border-blue-700 rounded-md text-black px-2' } }) }}
                {{ form_errors(resetForm.plainPassword) }}
            </div>
            {% if resetForm.plainPassword.first.vars.errors|length > 0 %}
                <div class="col-span-2 text-red-700 text-sm mb-2">
                    {{ form_errors(resetForm.plainPassword.first) }}
                </div>
            {% endif %}
            <p class="text-xs py-2 text-gray-400 text-justify">Entrez votre nouveau mot de passe. Assurez-vous qu'il contient au moins 8 caractères, y compris des majuscules, des chiffres et des symboles pour une meilleure sécurité.</p>
            <button class="block bg-linear-to-t from-sky-500 to-blue-700 hover:shadow-md hover:shadow-black text-white font-semibold rounded-3xl px-3 py-2 mx-auto text-sm cursor-pointer">Réinitialiser le mot de passe</button>
        {{ form_end(resetForm) }}
    </div>
{% endblock %}
